{
  "mcpServers": {
    "tos": {
      "command": "python3",
      "args": ["tos_mcp_server.py"],
      "env": {
        "TOS_ACCESS_KEY": "${TOS_ACCESS_KEY}",
        "TOS_SECRET_KEY": "${TOS_SECRET_KEY}",
        "TOS_REGION": "${TOS_REGION:-cn-beijing}",
        "TOS_ENDPOINT": "${TOS_ENDPOINT}"
      }
    }
  }
}